package utils;

public interface Endpoint {

    // Add your endpoints here. For endpoints that require a path parameter, simply add a {<insert param variable here>}

    String AUTH = "/auth/";
    String BOOKING = "/booking/";
    String BOOKING_ID = "/booking/{id}";
}
